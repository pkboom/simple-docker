```sh
docker run -it -p 3000:3000 simple-docker:latest

# -i: interactive, accept ctrl-c
# -t: tty
# -p: publish a container's port(s) to the host
# e.g. -p 3000:3000 => assign port 3000 on host machine to port 3000 on container
```
